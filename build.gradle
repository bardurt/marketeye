plugins {
    id 'java'
}

allprojects {
    repositories {
        mavenCentral()
        jcenter()
    }
}

dependencies {
    testImplementation group: 'junit', name: 'junit', version: '4.12'
    compile 'com.google.code.gson:gson:2.8.6'
}
